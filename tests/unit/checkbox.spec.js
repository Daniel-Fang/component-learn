import FyCheckbox from '@/components/checkbox';
import { shallowMount } from '@vue/test-utils';

describe('test checkbox action', () => {
    test('', () => {
        const wrapper = shallowMount(FyCheckbox);
        // console.log(wrapper);
    })
})